import React, { useState, useEffect } from 'react';
import BreedlistPicture from '@/components/BreedListPicture';
interface BreedPageImageProps {

}

const BreedPageImage: React.FC<BreedPageImageProps> = ({ }) => {

  return (
    <BreedlistPicture />
  );
};

export default BreedPageImage;